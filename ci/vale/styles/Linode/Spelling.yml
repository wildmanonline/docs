extends: spelling
message: "Did you really mean '%s'?"
level: error
filters:
  - '\W'
  # Matches any non-word characters.
  # To target just a possessive form, we could use `\w+'s`
  - '^.*_.*$'
  # Matches any word with an underscore
ignore: ../dictionary.txt
